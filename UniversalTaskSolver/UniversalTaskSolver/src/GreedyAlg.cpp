#include "GreedyAlg.h"

using namespace std;
using namespace slv;

// execute
std::vector<int> GreedyAlg::execute(TaskGraphPtr graph)
{
	/*
	if (!game)
		return vector<int>();

	// init
	m_game = game;
	m_graph = make_shared<grp::DynamicGraph>();
	*/

	// get root node
	// get k from neighbors nodes
	// select node
	// execute command
	// check
	// repeat

	return vector<int>();
}
